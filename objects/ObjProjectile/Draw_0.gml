draw_sprite_ext(sprite_index, 
				image_index, 
				x, y, 
				image_xscale, 
				image_yscale, 
				direction, 
				image_blend, 
				image_alpha);
if orbit
	draw_line(ObjPlayer.x, ObjPlayer.y, ObjPlayer.x+orbitRadius, ObjPlayer.y);